 
 export interface InfoPagina {
    // se le añade el sigo de interrogacion por que puede que sea un valor null
  titulo?: string;
  email?: string;
  nombre_corto?: string;
  github?: string;
  facebook?: string;
  twitter?: string;
  instagram?: string;
  tublr?: string;
  equipo_trabajo?: any[];
}

